{
  "name": "codefair-monorepo",
  "private": "true",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "bot:dev": "pnpm -C bot dev",
    "bot:build": "pnpm -C bot build",
    "ui:dev": "pnpm -C ui dev",
    "ui:build": "pnpm -C ui build",
    "dev": "pnpm --stream -r dev"
  }
}
